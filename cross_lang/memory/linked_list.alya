struct ListNode
    val, next
end

let count = 50000
let head = null
let i = 0
while i < count
    head = ListNode(i, head)
    i += 1
end

let sum = 0
let cur = head
while cur is not null
    sum += cur.val
    cur = cur.next
end

say sum
